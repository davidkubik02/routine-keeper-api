
export const getTasks = (req, res)=>{
    // const tasks = taskData.find(data=>data.username===req.user.username)
    // if (!tasks) return res.status(200).json([])
    // res.status(200).json(tasks.tasks)
}